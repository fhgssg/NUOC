import {TextStyle} from 'react-native';

export type TextThemeType = {
  heading1: {
    fontSize: number;
    fontWeight: string;
    color: string;
    lineHeight: number;
    fontFamily: string;
  };
  heading2: {
    fontSize: number;
    fontWeight: string;
    color: string;
    lineHeight: number;
    fontFamily: string;
  };
  heading3: {
    fontSize: number;
    fontWeight: string;
    color: string;
    lineHeight: number;
    fontFamily: string;
  };
  paragraph: {
    fontSize: number;
    fontWeight: string;
    color: string;
    lineHeight: number;
    fontFamily: string;
  };
  subText: {
    fontSize: number;
    fontWeight: string;
    color: string;
    fontFamily: string;
  };
  caption: {
    fontSize: number;
    fontWeight: string;
    color: string;
    fontFamily: string;
  };
  label: {
    fontSize: number;
    fontWeight: string;
    color: string;
    fontFamily: string;
  };
  buttonText: {
    fontSize: number;
    fontWeight: string;
    color: string;
    fontFamily: string;
  };
  linkText: {
    fontSize: number;
    color: string;
    textDecorationLine: 'underline';
    fontFamily: string;
  };
  smallText: {
    fontSize: number;
    color: string;
    fontFamily: string;
  };
};
